<Window x:Class="AutoSerivce.Views.Windows.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoSerivce.Views.Windows"
        mc:Ignorable="d"
        Title="Доступ к панели администратора" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="117*"/>
            <RowDefinition Height="167*"/>
        </Grid.RowDefinitions>

        <Image Source="/Resources/service_logo.png" Margin="5" />

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
            <TextBlock Text="Пароль" FontSize="14" />
            <TextBox Text="{Binding PassAdmin}"/>

            <Button Content="Авторизация" Command="{Binding AdminPasswordCommand}"/>
        </StackPanel>
    </Grid>
</Window>
